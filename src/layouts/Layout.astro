---
import '@fontsource/playfair-display/400.css';
import '@fontsource/playfair-display/700.css';
import '@fontsource/montserrat/400.css';
import '@fontsource/montserrat/600.css';
import Navigation from '../components/Navigation.astro';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Horus Productions - Production Créative de Films" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="bg-[#0a192f] text-white">
    <!-- En-tête avec logo et titre -->
    <header class="fixed top-0 left-0 right-0 p-4 bg-transparent flex items-center justify-between z-10">
      <!-- Logo -->
      <img src="/1045.png" alt="Horus Productions Logo" class="w-12 h-auto mr-4" />
      <!-- Titre injecté depuis la prop -->
      <h1 class="text-4xl font-bold text-white">{title}</h1>
    </header>

    <Navigation />
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --font-heading: 'Playfair Display', serif;
    --font-body: 'Montserrat', sans-serif;
  }
  html {
    scroll-behavior: smooth;
  }
  body {
    font-family: var(--font-body);
    overflow-x: hidden;
  }
  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-heading);
  }

  /* En-tête transparente, fixe en haut */
  header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: fixed;   /* Fixe l'en-tête en haut de la page */
    top: 0;            /* Positionne l'en-tête en haut */
    left: 0;
    right: 0;
    z-index: 10;       /* Assure que l'en-tête reste au-dessus des autres éléments */
    padding: 1rem;
    background-color: transparent; /* Fond transparent */
  }

  /* Pour éviter que le contenu soit caché sous l'en-tête */
  body {
    margin-top: 80px;  /* Ajoute un espace suffisant pour éviter que le contenu soit caché sous l'en-tête */
  }

  /* Style du titre */
  h1 {
    font-size: 2.5rem;  /* Taille du titre */
    color: white;  /* Couleur du texte */
  }
</style>
